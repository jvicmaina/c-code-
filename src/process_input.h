#ifndef PROCESS_INPUT_H
#define PROCESS_INPUT_H

#include <stdio.h> 

// Declare the prototype for the process_input function
void process_input(char *input, FILE *output_stream);

#endif /* PROCESS_INPUT_H */
